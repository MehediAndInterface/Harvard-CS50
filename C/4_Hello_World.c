#include <stdio.h> // Preprocessor directive: Tells the compiler to include the Standard Input Output header file, which contains the printf() function.

// int main()	The main function — the starting point of every C program.
int main() {
    printf("Hello, World!\n");
    
    return 0; // The return statement indicates that the program has finished executing successfully. The value 0 is returned to the operating system.
}
// Environment setup for C   
